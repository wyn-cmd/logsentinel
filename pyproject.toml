[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "logsentinel"
version = "0.1.0"
description = "A lightweight local log anomaly scanner and triage utility."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
logsentinel = "logsentinel.cli:main"
